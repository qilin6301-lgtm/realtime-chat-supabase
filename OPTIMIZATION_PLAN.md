# 即时通讯软件代码审查与优化方案

**审查对象：** `qilin6301-lgtm/realtime-chat-supabase`  
**审查时间：** 2026-09-10  
**审查范围：** React/Vite 前端、Supabase 数据模型与 RLS、实时消息链路、设备限制、礼物和财务功能、生产构建。

## 一、结论

当前项目已经具备即时通讯产品的主要功能骨架，包括邮箱 OTP 登录、用户资料、异性聊天限制、好友、广场、图片消息、表情、礼物、消息已读、编辑删除、设备限制和管理后台。项目适合继续迭代，但目前仍处于“可演示原型”阶段，不建议直接承载真实用户、真实余额或正式运营数据。

优化顺序应当是：**先修复发布阻断和权限安全，再保证消息与资金数据的一致性，然后优化核心交互，最后扩展增长和运营能力。** 如果反过来继续增加礼物、充值或社交功能，后续迁移和数据修复成本会明显上升。

## 二、当前状态摘要

| 项目 | 当前判断 | 依据 |
|---|---|---|
| 生产构建 | 阻断 | `npm run build` 因 `src/i18n/index.js` 含 JSX 但使用 `.js` 扩展名而失败 |
| 前端架构 | 可继续使用 | React 18 + Vite，组件按功能拆分，Supabase 客户端集中在 `src/lib/supabase.js` |
| 实时消息 | 已有基础链路 | 消息 INSERT/UPDATE 订阅、输入中广播、已读更新 |
| 数据权限 | 高风险 | 原始 README 中存在全表宽松策略；`rls_tighten.sql` 仍未覆盖所有敏感写操作 |
| 资金与礼物 | 高风险 | 余额由客户端计算并直接更新，消费记录和礼物发送不是事务 |
| 移动端 | 需要专项优化 | 现有媒体查询只处理了基础布局，聊天列表与聊天面板的导航状态不完整 |
| 可维护性 | 中等偏低 | 业务逻辑集中在组件中，缺少统一错误处理、类型约束、测试和 CI |

## 三、必须优先修复的问题

### 1. 修复生产构建

`src/i18n/index.js` 在第 140 行附近返回 JSX。Vite 已在构建阶段报错：该文件内容无法按普通 JavaScript 解析。最小修复是将文件重命名为 `src/i18n/index.jsx`，并确认所有导入路径仍由 Vite 正确解析。随后重新执行 `npm run build`。

建议同时增加以下发布门禁：

| 门禁 | 目标 |
|---|---|
| `npm run build` | 必须成功 |
| ESLint 或等价静态检查 | 不允许新增错误 |
| 基础单元测试 | 覆盖权限判断、消息排序、余额计算边界 |
| CI | Pull Request 必须完成构建和测试 |

### 2. 收紧 Supabase RLS 和字段级写权限

当前风险不是单一策略错误，而是“客户端可直接写入敏感字段”的设计问题。`rls_tighten.sql` 中的 `profiles_update_own` 允许用户更新自己的整行资料，因此理论上用户可以尝试修改 `is_admin`、`balance`、`max_devices` 和 `is_approved`。这些字段必须从客户端可写路径中移除。

建议采用以下边界：

| 数据 | 客户端允许 | 仅服务端或管理员允许 |
|---|---|---|
| `profiles.username`、`bio`、`avatar_url`、`country` | 本人更新 | 管理员可审核 |
| `profiles.is_admin` | 不允许 | 数据库迁移或受控后台 |
| `profiles.balance` | 不允许直接更新 | 数据库函数或支付服务 |
| `profiles.max_devices` | 不允许本人更新 | 管理员或服务端函数 |
| `profiles.is_approved` | 不允许本人更新 | 管理员审核函数 |
| `invite_codes` 的使用状态 | 不允许直接改整行 | 原子化兑换函数 |
| `messages` | 发送本人消息；编辑/删除本人消息 | 已读状态应走专用函数 |
| `message_likes` | 本人点赞或取消自己的点赞 | 计数由数据库维护 |
| `recharge_records`、`consume_records` | 不允许伪造写入 | 服务端记账函数 |

`messages_update` 当前对任意会话参与者开放，参与者可以修改消息的内容、发送者、礼物标识和其他字段。应改成数据库函数或字段级约束，只允许发送者在限定时间内修改正文或软删除；接收者只能通过专用“标记已读”函数更新 `read_at`。

`invite_codes` 不应让所有已登录用户读取全部邀请码。注册时应调用一个原子函数，根据邀请码完成校验、占用和用户审核状态更新，避免两个注册请求同时消费同一个邀请码。

### 3. 将余额和送礼流程改为数据库事务

`ChatWindow.jsx` 的 `sendGift` 先在客户端计算余额，再分别更新 `profiles`、插入消费记录、插入送礼记录和插入消息。这会产生以下问题：重复点击可能重复扣款；并发请求可能造成余额覆盖；中途失败会出现余额已扣但礼物或消息未写入；恶意客户端可以绕过前端直接修改余额。

建议新增数据库函数，例如 `send_gift(p_gift_id, p_receiver_id, p_conversation_id)`，在一个事务中完成：

1. 锁定发送者余额行。
2. 校验礼物仍处于启用状态。
3. 校验发送者和接收者满足聊天规则。
4. 校验余额足够。
5. 扣减余额。
6. 写入消费记录和礼物发送记录。
7. 写入消息。
8. 返回新的余额和消息 ID。

函数应使用幂等键或客户端请求 ID，防止网络重试造成重复扣款。前端只调用该函数，不再直接更新 `profiles.balance`。

### 4. 统一会话主键，避免重复会话

`conversations` 通过 `unique (user1_id, user2_id)` 防止重复，但没有保证两个用户 ID 的顺序一致。A 发起 B 和 B 发起 A 可能生成两条会话。建议增加数据库约束或函数，始终把较小 UUID 放在 `user1_id`，较大 UUID 放在 `user2_id`，并通过 `get_or_create_conversation` 统一创建。

## 四、消息系统的可靠性优化

### 1. 修复实时消息重复和排序问题

当前 INSERT 订阅直接执行 `setMessages(prev => [...prev, msg])`，没有检查消息是否已经存在。网络重连、订阅重建或多次加载后可能出现重复消息。建议按消息 ID 去重，并以 `created_at` 和 ID 作为稳定排序键。

当前查询固定取最早方向的 200 条消息，然后自动滚动到底部。会话较长后用户无法查看更早历史，且每次打开都要加载不必要的数据。建议改为：

- 初始加载最近 50 条。
- 向上滚动时按游标加载更早消息。
- 使用 `created_at < cursor`，而不是深分页 offset。
- 只在用户接近底部时自动滚动。
- 用户阅读旧消息时显示“回到底部”和未读数量。

### 2. 增强发送状态

当前发送失败只弹出 `alert`，发送中的消息没有状态，也没有重试入口。建议引入 `pending`、`sent`、`failed` 三种本地状态，给失败消息提供“重试”和“复制文本”操作。服务端消息应记录客户端请求 ID，前端通过幂等写入避免重复。

### 3. 限制输入中广播

`broadcastTyping` 在每次输入时发送实时事件，长时间输入会产生大量广播。应使用 300–500 毫秒节流，并在停止输入后发送一次结束状态，或使用短 TTL 的 presence 状态。频道名称还应包含用户身份校验逻辑，避免无关用户观察到输入状态。

### 4. 正确处理已读

`markAsRead` 将会话中所有由对方发送且未读的消息一次性标记为已读。更准确的实现是只更新当前用户真正看到的消息，或者写入“最后已读消息 ID”。后者比逐条更新更节省数据库写入，并且更容易支持多设备同步。

## 五、文件与内容安全

聊天图片目前上传到公开的 `posts` bucket，并把公开 URL 直接作为消息内容。对于私聊图片，这会导致获得 URL 的任何人都可能访问内容。建议：

- 私聊图片使用私有 bucket。
- 通过短时限 signed URL 展示。
- 校验 MIME 类型、实际文件内容和文件尺寸。
- 对图片做压缩、缩略图和 EXIF 清理。
- 使用随机对象名，避免暴露用户 ID 和时间戳。
- 在数据库中保存文件对象 ID，而不是永久公开 URL。

用户名、个人简介、动态和消息也应增加长度限制、非法字符处理、链接策略和举报能力。React 默认会转义 HTML，但仍应防范过长内容、恶意链接和图片外链滥用。

## 六、前端体验优化

### 1. 移动端聊天导航

当前媒体查询把 sidebar 设为绝对定位，但没有清晰的“列表态/聊天态”路由或可访问的返回逻辑。建议在移动端将会话列表和聊天窗口设计为两个明确状态：选择会话后进入聊天页，返回后回到列表页，并保留浏览器返回键行为。

### 2. 加载、空状态和错误状态

多个查询只读取 `data`，没有统一处理 `error`。网络失败时用户容易看到空列表并误以为没有数据。建议建立统一的 `useAsyncState` 或请求封装，区分加载中、成功为空、失败和重试四种状态。

### 3. 通知和未读体验

现有浏览器通知已经有基础实现，但还应增加：会话未读计数、页面可见性判断、声音开关、免打扰时间段、当前会话不重复通知，以及多标签页去重。

### 4. 无障碍和国际化

按钮目前大量依赖符号字符，例如返回、点赞和性别图标。应增加 `aria-label`、键盘焦点样式和语义化按钮名称。国际化语言文件中存在多个仅包含少量内容的语言文件，建议先明确支持语言范围，避免界面混用中文和英文。

## 七、后端与数据模型建议

建议补充以下字段和索引：

| 表 | 建议字段或索引 | 目的 |
|---|---|---|
| `messages` | `(conversation_id, created_at desc)` | 最近消息和游标分页 |
| `messages` | `client_request_id unique` | 幂等发送 |
| `messages` | `deleted_at`、`delete_reason` | 审计和软删除 |
| `conversations` | 规范化用户对约束 | 防止重复会话 |
| `conversations` | `last_read_message_id` 或独立 read state | 高效多设备已读同步 |
| `user_sessions` | `revoked_at`、`last_ip_hash`、`user_agent` | 设备管理和安全审计 |
| `profiles` | `updated_at` | 缓存和审计 |
| 财务记录 | `idempotency_key`、`balance_after` | 对账和重复请求防护 |
| 举报表 | `reporter_id`、`target_type`、`target_id`、`status` | 风控和人工审核 |

数据库应为高频查询添加索引，并使用服务端聚合统计替代管理后台一次性读取全部 `profiles` 后在浏览器计算。管理员查询还应支持分页，避免用户量增长后页面性能下降。

## 八、推荐的实施排期

| 阶段 | 目标 | 主要工作 | 完成标准 |
|---|---|---|---|
| P0，1–2 天 | 可构建、不可越权 | 修复 `.jsx` 扩展名；收紧 profiles、messages、finance、invite RLS；暂停客户端余额更新 | 构建通过；匿名用户和普通用户无法越权修改敏感字段 |
| P1，3–5 天 | 消息可靠 | 会话规范化；消息去重；最近消息游标分页；发送状态和重试；已读模型优化 | 连续重连不重复；长会话可分页；失败消息可恢复 |
| P2，3–5 天 | 资金可用 | 数据库送礼事务；幂等键；私有图片和 signed URL；审计日志 | 重复点击不重复扣款；异常可对账；私聊图片不公开 |
| P3，5–7 天 | 核心体验 | 移动端双状态导航；未读数；统一错误页；输入广播节流；通知策略 | 移动端可单手完成主要流程，错误可重试 |
| P4，持续迭代 | 运营和增长 | 举报、拉黑、风控、搜索、推荐、埋点、灰度发布 | 能识别留存、会话建立率、首条消息回复率等指标 |

## 九、建议的产品指标

上线前不要只看注册人数。建议埋点并观察以下指标：注册完成率、首次进入好友页的比例、发起首个会话的比例、首条消息发送成功率、首条消息在 24 小时内收到回复的比例、次日和七日留存、消息失败率、通知点击率、举报率、图片上传失败率、礼物交易成功率和财务对账差异率。

对当前产品而言，最重要的北极星路径可以定义为：**新用户完成资料后，在 10 分钟内成功建立一个会话并发送第一条消息，且对方在 24 小时内回复。** 优化应围绕这条路径减少等待、减少不确定状态和减少权限错误。

## 十、建议立即执行的首批任务

1. 将 `src/i18n/index.js` 重命名为 `src/i18n/index.jsx`，重新构建并加入 CI。
2. 删除客户端直接更新 `profiles.balance` 的路径。
3. 为 profile 敏感字段拆分更新策略，禁止用户自改 `is_admin`、`balance`、`is_approved` 和 `max_devices`。
4. 重写消息更新策略，只允许发送者编辑或删除自己的消息，并限制编辑时间窗口。
5. 创建 `get_or_create_conversation` 和 `send_gift` 数据库函数。
6. 将消息图片迁移到私有 bucket，并使用 signed URL。
7. 增加消息 ID 去重、最近消息游标分页和发送失败重试。
8. 为构建、权限、送礼事务和设备限制补充自动化测试。

## References

[1]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase Row Level Security Documentation"
[2]: https://supabase.com/docs/guides/database/functions "Supabase Database Functions Documentation"
[3]: https://supabase.com/docs/guides/storage/security/access-control "Supabase Storage Access Control Documentation"
[4]: https://vite.dev/guide/ "Vite Guide"
